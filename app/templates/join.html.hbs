<section class="body">
    <select name="servers" id="join_server-name">
    {{#each servers}}
        <option value="{{ @key }}">{{ this }}</option>
    {{/each}}
    </select>
    <input type="text" id="join_channel-name"/>

    <button class="green btn" id="join-chan">join channel</button>
</section>