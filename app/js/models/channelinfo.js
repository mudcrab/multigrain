window.Multigrain = window.Multigrain || {};
Multigrain.Model = Multigrain.Model || {};

(function($) {
	Multigrain.Model.ChannelInfo = Backbone.Model.extend({

		defaults: {
			channel: 'N/A'
		},

		initialize: function(options) 
		{
			// 
		}

	});
})();
	